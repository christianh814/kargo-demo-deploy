configVersion: v1alpha1
branchConfigs:
- pattern: env/(\w+)
  appConfigs:
    kargoDemo:
      configManagement:
        kustomize:
          path: env/${1}
      outputPath: ${1}
      combineManifests: true
