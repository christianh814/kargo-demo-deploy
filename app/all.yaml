apiVersion: v1
kind: Service
metadata:
  labels:
    app: kargo-demo-app
  name: kargo-demo-app
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: kargo-demo-app
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: kargo-demo-app
  name: kargo-demo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kargo-demo-app
  template:
    metadata:
      labels:
        app: kargo-demo-app
    spec:
      containers:
      - image: quay.io/christianh814/kargo-demo-app:940314
        name: kargo-demo-app
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  labels:
    app: kargo-demo-app
  name: kargo-demo-ing
spec:
  ingressClassName: nginx
  rules:
  - host: test.7f000001.nip.io
    http:
      paths:
      - backend:
          service:
            name: kargo-demo-app
            port:
              number: 8080
        path: /
        pathType: Prefix
